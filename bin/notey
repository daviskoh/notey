#!/usr/bin/env node

'use strict';

var path = require('path'),
    lib = path.join(path.dirname(fs.realpathSync(__filename)), '../lib'),
    // requiring the file executes module (see main.js)
    require(lib + '/main.js');
